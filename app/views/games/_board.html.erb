<div class="d-flex flex-wrap justify-content-center" style="width: 300px; margin: 0 auto;">
  <% game.state.each_with_index do |value, index| %>
    <div style="width: 100px; height: 100px;" class="border d-flex align-items-center justify-content-center">
      <% if value.present? || game.finished? %>
        <strong style="font-size: 2rem;"><%= value %></strong>
      <% elsif game.current_player == "X" %>
        <%= button_to "🞂", move_game_path(game, index: index),
              method: :post,
              form: { data: { turbo_stream: true } },
              class: "btn btn-outline-primary btn-lg",
              style: "width: 100%; height: 100%; padding: 0;" %>
      <% else %>
        <!-- C’est au tour de l’IA ou partie terminée -->
        <span>&nbsp;</span>
      <% end %>
    </div>
  <% end %>
</div>
